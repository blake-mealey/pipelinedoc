name: Stages (parameters & properties)
version: 1
description: |
  A stages template with parameters and a properties file.
parameters:
  name:
    description: |
      A useless parameter
      with a multi-line
      description.

      And a second paragraph!
